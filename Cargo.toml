[package]
name = "life-support"
version = "0.1.0"
edition = "2021"

[dependencies]
askama = "0.12"
axum = "0.8"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net", "signal"] }
tower-http = { version = "0.6", features = ["fs"] }
